<h1>Edit Task</h1>

<%= form_with(model: @task) do |f| %>

  <%= render partial: 'form', locals: {f: f, sections: @project.sections, collaborators: @collaborators} %>

  <%= f.submit "Update Task"%>

<% end %>